# MySQL

: 관계형 데이터베이스 관리 시스템(RDBMS: Relational DBMS) / 오픈소스

> 표준 데이터베이스 질의(질문) 언어 SQL(Structured Query Language)을 사용하는 개방 소스의 관계형 데이터베이스 관리 관리시스템(RDBMS), 매우 빠르고, 유연하며, 사용하기 쉬운 특징이 있다.



> 데이터베이스란?

+ 어느 한 조직의 여러 응용 시스템이 공유할 수 있도록 통합, 저장된 운영 데이터의 집합
+ 데이터베이스에서는 어떤 목적을 가지고 있는 하나의 테이블로 정의

> DB를 사용하는 이유?
>
> 

> 데이터베이스 관리 시스템(DBMS)

​			--> 데이터의 독립성을 제공하는 것.

+ 다수의 사용자들이 데이터베이스 내의 데이터를 접근할 수 있도록 해주는 소프트웨어 도구의 집합.
+ MySQL도 DBMS의 한종류



> RDMBS(Relational Database Management System)
>
> 관계형 데이터베이스를 생성, 갱신, 관리하기 위한 시스템

____

> MySQL

+ MySQL에서 데이터베이스에 대한 작업 명령은 **SQL 구문을 이용하여 처리**

  \1. # 한 줄 주석

  \2. -- 한 줄 주석

  \3. /* 두 줄

    이상의

    주석 */

\1. CREATE DATABASE  : 새로운 데이터 베이스를 생성해줌

\2. ALTER DATABASE : 데이터베이스의 내용을 수정할 수 있음.

\3. CREATE TABLE : 새로운 테이블 생성

\4. ALTER TABLE : 테이블의 내용을 수정

> TABLE은 정렬된 데이터 집합(값)의 모임
>
> 테이블은 행(row)와 열(column)로 이루어짐.
>
> 데이터베이스 안에 각각의 테이블로 정리됨.

...



____

### +프라이머리키(Primary Key) :기본키

관계형 데이터베이스에서 조(레코드)의 식별자로 이용하기에 가장 적합한 것을 관계 (테이블)마다 단 한 설계자에 의해 선택, 정의된 후보 키

+ Primary key(이하 PK)는 한 개 혹은 여러 개의 칼럼으로 테이블 내의 각 행들을 구별하기 위한 목적을 갖고 있다.

+  PK는 그 자체만으로 독특하며 만약 여러 개의 칼럼이 PK로 묶여 있다면, 해당 값들의 조합이 반드시 독특해지게 된다.

+ PK는 NULL 값을 갖을 수 없고(NOT NULL), Unique key라는 특징을 갖게 된다. 단순하게는 NOT NULL & UNIQUE == PK 라고 생각하면 편하다. 물론 좀더 디테일하게 보자면 차이가 있다.

  

### +ORM(Object relational Mapping)

> Object Modeling은 OOP(객체 지향 프로그래밍)을 기반으로 생성된 시스템

> Relational Modeling은 서술어와 비슷한 truth statement(진실 진술)로         정보를 표현

> ORM이란?
>
> 

